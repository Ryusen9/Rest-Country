const arr = [34, 35, 45, 65];
const person = {
    name: 'John Doe',
    age: 30,
    city: 'New York',
}

const info = `Hi, my name is ${person.name}, I am ${person.age} and I live in ${person.city}. I got ${arr[3]} in math.`
console.log(info);